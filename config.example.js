// EcoSpark AI configuration
// 1. Copy this file and rename the copy to: config.js
// 2. Paste your Gemini API key below (get one at https://aistudio.google.com)
// 3. config.js is gitignored so your key never ends up in version control.
//    Reminder: anything shipped to the browser is visible to visitors —
//    use a key restricted to your site's domain, and rotate it after demos.
// Without a key, EcoSpark still works using its built-in eco knowledge base.

window.ECOSPARK_CONFIG = {
  GEMINI_API_KEY: "PASTE_YOUR_GEMINI_API_KEY_HERE",
};
